# Author: PeerHeer
#
# Start iteration by initializing the iterator and then iterating over the iterable.

# Initialize iterator.
function dynalist.private:operations/delete/delete_index/initialize_iterator

# Start the iteration if the list is not empty.
function dynalist.private:operations/delete/delete_index/iterate

# Set success to 1.
scoreboard players set $dynalist.success dynalist.out 1