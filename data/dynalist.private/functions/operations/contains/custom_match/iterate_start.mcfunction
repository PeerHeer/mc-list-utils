# Author: PeerHeer
#
# Start iteration by initializing the iterator and then iterating over the iterable.

# Initialize iterator.
function dynalist.private:operations/contains/custom_match/initialize_iterator

# Start the iteration if the list is not empty.
function dynalist.private:operations/contains/custom_match/iterate

