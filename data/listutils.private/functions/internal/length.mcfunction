# Author: PeerHeer
#
# Returns the length of the list.

# Store the length of the list in $listutils.length
execute store result score $listutils.length listutils.var run data get storage listutils:in List